# ============================================================================
# Postiz Docker Compose Production Configuration
# ============================================================================
#
# IMPORTANT:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual secrets
# 3. Generate strong passwords using: openssl rand -base64 32
# 4. Keep .env file secure: chmod 600 .env
# 5. Never commit .env to version control
#
# ============================================================================

# === DOMAIN CONFIGURATION ===
# Your domain name (e.g., postiz.example.com)
DOMAIN=postiz.yourdomain.com

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL user
DB_USER=postiz

# PostgreSQL password (CHANGE THIS! min 20 characters)
# Generate with: openssl rand -base64 32
DB_PASSWORD=change-me-to-a-secure-random-password-min-20-chars

# PostgreSQL database name
DB_NAME=postiz_db

# ============================================================================
# REDIS CONFIGURATION
# ============================================================================

# Redis password (CHANGE THIS! min 20 characters)
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=change-me-to-a-secure-random-password-min-20-chars

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# JWT Secret for token signing (CHANGE THIS! min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=change-me-to-a-strong-jwt-secret-min-32-chars

# Encryption key for storing sensitive data (CHANGE THIS! min 32 characters)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=change-me-to-a-strong-encryption-key-min-32-chars

# ============================================================================
# EMAIL/SMTP CONFIGURATION (Optional)
# ============================================================================
# Leave empty if you don't need email functionality

# SMTP server hostname
SMTP_HOST=smtp.yourmailserver.com

# SMTP server port (usually 587 for TLS or 25 for unencrypted)
SMTP_PORT=587

# SMTP username/email
SMTP_USER=your-email@yourdomain.com

# SMTP password
SMTP_PASSWORD=your-email-password

# Email address to send from
SMTP_FROM=noreply@yourdomain.com

# Email display name
SMTP_FROM_NAME=Postiz

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level: trace, debug, info, warn, error
LOG_LEVEL=info

# ============================================================================
# PERFORMANCE CONFIGURATION
# ============================================================================

# Number of worker processes (adjust based on CPU cores)
MAX_WORKERS=4

# ============================================================================
# OPTIONAL: AI PROVIDER CONFIGURATION
# ============================================================================
# Uncomment and fill these if you want to use AI features with your custom security enhancements

# OpenAI API Key
# OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic Claude API Key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google Gemini API Key
# GEMINI_API_KEY=your-gemini-key-here

# Together AI API Key
# TOGETHER_API_KEY=your-together-key-here

# ============================================================================
# OPTIONAL: SOCIAL MEDIA INTEGRATION
# ============================================================================
# Uncomment and fill these if you want to integrate with social platforms

# Twitter/X API Configuration
# TWITTER_API_KEY=your-twitter-key
# TWITTER_API_SECRET=your-twitter-secret

# Google OAuth Configuration (for YouTube, etc.)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook/Meta Configuration
# FACEBOOK_APP_ID=your-facebook-app-id
# FACEBOOK_APP_SECRET=your-facebook-app-secret

# Instagram Configuration (via Facebook)
# INSTAGRAM_BUSINESS_ACCOUNT_ID=your-instagram-account-id

# LinkedIn Configuration
# LINKEDIN_CLIENT_ID=your-linkedin-client-id
# LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret

# TikTok Configuration
# TIKTOK_CLIENT_ID=your-tiktok-client-id
# TIKTOK_CLIENT_SECRET=your-tiktok-client-secret

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your values:
#    nano .env
#
# 3. Generate strong passwords (run these commands):
#    openssl rand -base64 32  # DB_PASSWORD
#    openssl rand -base64 32  # REDIS_PASSWORD
#    openssl rand -base64 32  # JWT_SECRET
#    openssl rand -base64 32  # ENCRYPTION_KEY
#
# 4. Set secure file permissions:
#    chmod 600 .env
#
# 5. Build and start Postiz:
#    docker-compose -f docker-compose.prod.yml up -d
#
#    This will:
#    - Build your Docker image from Dockerfile.production (includes all your security fixes)
#    - Start PostgreSQL container
#    - Start Redis container
#    - Start Postiz application container
#
# 6. Run database migrations:
#    docker-compose -f docker-compose.prod.yml exec postiz-app pnpm run prisma-db-push
#
# 7. Access your Postiz instance:
#    https://postiz.yourdomain.com
#    (after Nginx reverse proxy is configured in Centminmod)
#
# ============================================================================
